@model Categories
@{
    ViewData["Title"] = "Index";
}

<h1>Kategoriler</h1>

<div class="list-group">
@foreach (var item in Model.Name)
{
        <a href="#" class="list-group-item list-group-item-action">@Model.Name</a>
}

</div>